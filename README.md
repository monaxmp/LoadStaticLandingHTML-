# LoadStaticLandingHTML-

1. Скачиваем `wget -r -k -l 7 -p -E -nc http://site.com/`
2. Файл должен быть unminify (обрабатывается построчно)
3. Можно использовать для преобразования путей (в регулярном выражении убрать `http`)
